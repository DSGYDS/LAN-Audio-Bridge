## LAN Audio Bridge — 项目进度存档

> 最后更新：2026-07-23
> 
> **当前阶段**：WiFi 直连大重构（P0-P2 已完成，P3 待启动）
> **项目状态**：僵尸清理/链路分离/API合规 已验收，接口层补全待启动
> **GitHub 仓库**：github.com/DSGYDS/LAN-Audio-Bridge（公开）
>
> **Windows 端调试日志**：`windows/LanAudioBridge.Desktop/bin/Debug/net10.0-windows10.0.19041.0/debug.log`
> （ConsoleLogger 自动写入，含 AudioEngine 帧统计、JitterBuffer 丢帧原因、HandshakeServer 握手记录，每次启动追加写入）

---

## 项目定位

一句话：手机音频零配置、低延迟、多模式传到电脑播放的局域网工具。

当前成熟度：**核心链路稳定，功能闭环**
- 核心链路已通（Android 采集 → Opus 编码 → UDP → Windows 解码 → 播放）✅
- 四层架构重构完成（接口层/适配器层/基础设施层/应用层）✅
- FEC 丢包补偿 + JitterBuffer 抖动缓冲 + 热切防抖 ✅
- 断线重连闭环（看门狗 + 状态机 + 5次重试）✅
- 待完成：四级链路降级、实时仪表盘 UI

---

## ✅ 已完成功能

### Phase 1：Python 原型验证（100%）
- 麦克风采集、PCM UDP回环、Opus编解码、全链路联调

### Phase 2：Android 端音频采集（100%）
| 关键产出 | 说明 |
|---------|------|
| MicrophoneCapturer.kt | AudioRecord 48kHz/16bit/20ms + 200Hz HPF |
| SystemAudioCapturer.kt | MediaProjection 系统音频采集 |
| AudioEncoder.kt | Concentus Opus编码（128kbps/FEC/CVBR） |
| AudioMixer.kt | 两路PCM叠加混音 |

### Phase 3：Windows 端音频播放（100%）
| 关键产出 | 说明 |
|---------|------|
| AudioEngine.cs | ITransport接收 → IPacketProtocol解码 → Opus解码 → FEC补偿 → JitterBuffer → Router分发 |
| AudioRouter.cs | 四模式分发（Speaker/MicOnly/Both/MicOnlySys） |
| JitterBuffer.cs | 按seq排序、3帧预缓冲、溢出保护、新会话自动重置 |

### Phase 4：网络传输 + 设备发现（100%）
- 14B PacketHeader统一协议（双端实现）
- UDP 12345音频 + 12347控制信令
- mDNS自动发现（Android NsdManager + Windows Makaretu.Dns）
- 二进制握手协议（HELLO/HELLO_ACK/ROUTE/ROUTE_ACK）
- 断线重连（Android 5次重试 + Windows 3秒Watchdog）
- 连接状态机（ConnectionStateManager双端，9态完整转换表）

### Phase 5：四条管线完整调通（100%）
- 🔊 模式1 Speaker：系统音频 → 扬声器
- 🎛️ 模式2 Monitor：系统音频+麦克风 → 扬声器
- 🎤 模式3 Virtual Mic：麦克风 → VB-CABLE虚拟麦克风
- 🎮 模式4 Shenanigans：系统音频 → VB-CABLE虚拟麦克风
- 🔄 热切换：推流中任意切换模式（150ms防抖）

### Phase 5.5：传输层基建加固（100%）
- 连接状态机 / 协议头抽象 / 断线重连 / 延迟调优（~200ms端到端）

### Phase 6：VB-CABLE驱动集成（100%）
- 安装/卸载脚本 + 驱动包存档

---

## 🏗️ 传化层（平台抽象层）重构 — 全部完成 ✅

### P0-P3：基础设施就位（100%）
| Phase | 内容 | 状态 |
|-------|------|------|
| P0 | Core/目录搭建（7子目录/端） | ✅ |
| P1 | ILogger / IPacketProtocol / ITransport接口+桩实现 | ✅ |
| P2 | Infrastructure（Logger门面） | ✅ |
| P3 | PlatformFactory + Enums | ✅ |

### P4：ITransport 全链路接入（100%）✅
| 步骤 | 内容 | 状态 |
|------|------|------|
| 1 | Windows UdpTransport 补 server 模式回复 | ✅ |
| 2 | AudioEngine 接入 ITransport + IPacketProtocol | ✅ |
| 3 | HandshakeServer 接入 ITransport + IPacketProtocol | ✅ |
| 4 | MainWindow 注入 Transport（PlatformFactory） | ✅ |
| 5 | Android UdpTransport 补 sendBlocking + 随机端口 | ✅ |
| 6 | AudioPipeline 接入 ITransport + IPacketProtocol | ✅ |
| 7 | HandshakeManager 接入 ITransport + IPacketProtocol | ✅ |
| 8 | MainActivity 无需改动（API 不变） | ✅ |
| 9 | 删除 UdpSender.kt（废弃） | ✅ |
| 10 | 双端编译通过 + 四管线功能验证 | ✅ |

**最终状态**：双端应用层完全通过 ITransport/IPacketProtocol 接口通信，PacketHeader 直接调用仅存在于 PacketHeaderAdapter 内部，所有 Socket 操作封装在 UdpTransport 适配器中。

### P4 后质量修复（100%）✅
| Fix | 内容 | 状态 |
|-----|------|------|
| 1 | 状态机转换表修复（Idle/Disconnected/Reconnecting → Connecting） | ✅ |
| 2 | Opus FEC 丢包补偿（seq 追踪 + 单帧恢复） | ✅ |
| 3 | JitterBuffer（3帧预缓冲 + 溢出保护 + 新会话重置 + 热切150ms防抖） | ✅ |

### 代码清理（100%）✅
| 优先级 | 内容 | 状态 |
|--------|------|------|
| P0 | 删除僵尸文件（Lpf.cs/Config.cs/Config.kt）+ 僵尸方法/属性/常量 | ✅ |
| P1 | 性能优化（UdpTransport零拷贝、PacketHeader.TryDecode改ReadOnlySpan、消除冗余ToArray） | ✅ |
| P2 | 代码规范（无意义await/using/字符串插值） | ✅ |

---

## 🛠️ WiFi LAN 直连优化进度（2026-07-23）

### ✅ 已完成

#### 1. 冷启动一帧即停修复
**根因**：JitterBuffer 的 `Pull()` 在 underrun 时无条件执行 `_nextPullSeq++`，冷启动时 HAL 帧到达间隔 ~100ms（正常 20ms），`_nextPullSeq` 飞速前进，后续到达的帧全部被 `Push()` 判定为“迟到包”（`SeqBefore(seq, _nextPullSeq)` → backward < 100）丢弃，形成恶性循环。

**修复**（`JitterBuffer.cs`）：
- underrun 不再立即推进 `_nextPullSeq`，改为连续 N 次后才跳过
- 冷启动阶段（前 100 次拉取 ≈ 2s）：阈值 = 25（500ms 容忍）
- 正常阶段：阈值 = 5（100ms 容忍）
- 成功拉取后重置计数器

#### 2. HELLO 会话重置
**根因**：Android 每次 HELLO 后重启推流（seq 从 0 开始），但 Windows 端 JitterBuffer 的 `_nextPullSeq` 仍停留在上一会话（如 97），新会话 seq=0 被判定为迟到包丢弃。

**修复**（`AudioEngine.cs` + `MainWindow.axaml.cs`）：
- 新增 `AudioEngine.ResetSession()` 方法，重置 JitterBuffer + seq 追踪 + 看门狗状态
- `OnHandshakeRoute` 收到 HELLO 时调用 `ResetSession()`

#### 3. 冷启动淡入
**目的**：掩盖 HAL 冷启动初始 underrun 静音间隙。

**实现**（`AudioEngine.cs`）：
- 每次 `ResetSession()` 后，前 100 帧（≈2s）音量从 0 线性渐变到 1
- 在 `PlaybackTick()` 中对 PCM 数据乘以增益系数

#### 4. Android Opus 编码器预热
**根因**：Opus 编码器首次调用触发 JIT 编译，阻塞采集线程 ~2.3 秒。

**修复**（`AudioEncoder.kt`）：
- `prepare()` 中新增一帧静音预编码，提前触发 JNI JIT 编译

#### 5. Android HAL 预热
**修复**（`MicrophoneCapturer.kt` / `SystemAudioCapturer.kt`）：
- 新增 `warmup()` 方法，采集线程启动后丢弃前 3 帧（≤50ms）
- 新增 `restart()` 方法，看门狗触发时重建 AudioRecord

#### 6. P0 管线被杀自动恢复（已实现，待部署验证）
- 帧时序看门狗：连续 15 次 null（≈300ms）触发 capturer restart，最多 5 次
- Partial WakeLock（2h 上限）+ AudioFocus（AUDIOFOCUS_GAIN）
- AndroidManifest 添加 WAKE_LOCK 权限

#### 7. 诊断日志系统
- `ConsoleLogger` 增加文件日志输出（`debug.log`）
- `AudioEngine` 每 5s 输出帧统计（recv/decOk/pullOk/jbCount/prefilled/speakerReady）
- `JitterBuffer` 输出 Push 丢弃原因（late/dup/overflow）和 Pull GAP 检测

### 🔲 待收尾优化（UI 完成后、打包发布前必须完成）

#### 管线2（Monitor）异常调用虚拟麦克风
**现象**：管线2（系统音频+麦克风→扬声器）模式下会异常触发 CABLE Input（虚拟麦克风）设备。
**预期**：管线2 应仅输出到扬声器（SpeakerOnly + Mic 混音），不应启动 WasapiOut/CABLE。
**优先级**：B 级（功能异常但不影响核心链路）
**状态**：待修复（P3 验收时确认存在）

#### 冷启动初始卡顿深度优化
**现状**：冷启动最初 ~1-2 秒仍有轻微卡顿（已通过淡入掩盖，用户可接受）。

**根因**：Android Audio HAL 冷态导致前 1-2 秒帧到达间隔不稳定（~50-150ms 而非正常 20ms），Windows 端 JitterBuffer 在初始阶段出现 underrun（缓冲耗尽）。

**后期优化方向**：
1. **Android 端增强 warmup**：增加预热帧数（当前 3 帧 → 10+ 帧），确保 HAL 完全就绪后再开始正式采集
2. **Android 端 Opus 编码器深度预热**：在 `prepare()` 中预编码多帧（触发完整 JIT 编译路径）
3. **Windows 端自适应 preFill**：冷启动时动态提高 preFillCount（如检测到帧到达间隔 > 50ms 时提高至 15-20），稳定后降回 5
4. **Windows 端 PLC（丢包隐藏）**：underrun 时用 Opus PLC 生成 comfort noise 而非纯静音
5. **Android 端采集线程提权**：`Process.setThreadPriority(THREAD_PRIORITY_URGENT_AUDIO)` 减少初始帧调度抖动

#### 推流中切出/冷启动其他 App 导致卡顿
**现状**：推流中切桌面或冷启动其他应用时，音频出现短暂卡顿（进程不再被杀，但帧发送节奏被打乱）。

**根因**：
1. **GC 暂停**：`readFrame()` 每帧分配新 `ByteArray(1920)`，导致频繁 GC（每 2-3s 一次），单次 GC 暂停可达 **185ms**
2. **CPU 抢占**：其他 App 冷启动占用大量 CPU，采集线程调度延迟，帧突发到达 Windows 端

**修复方案**：
1. **缓冲区复用**：`readFrame()` 改为复用预分配的 ByteArray，消除每帧 GC 压力
2. **采集线程提权**：`Process.setThreadPriority(THREAD_PRIORITY_URGENT_AUDIO)` 确保 CPU 抢占时采集线程优先调度
3. **Windows 端 JitterBuffer 自适应**：检测到帧突发时动态加深缓冲，平滑突发输出

---

## 🏗️ WiFi 直连大重构（2026-07-23 启动）

### 重构总览

| 阶段 | 内容 | 状态 |
|------|------|------|
| **P0** | 僵尸清理 + 缺陷修复 | ✅ 已验收 |
| **P1** | 四级链路高度分离 | ✅ 已验收 |
| **P2** | API 合规性审查 + 修正 | ✅ 已验收 |
| **P3** | 接口层补全（IAudioCapturer/IAudioRenderer/IDiscovery/INetworkMonitor/Config） | ✅ 已验收 |
| **P4** | 插件化扩展接口预留（IDataChannel/IStreamHandler） | ✅ 已验收 |
| **P5** | 应用层重构接入（Pipeline/Router/Reconnection 改用接口） | 🔲 待启动 |
| **P6** | 回归验证（全量功能 + 清单闭环） | 🔲 待启动 |

### P0：僵尸清理 + 缺陷修复 ✅

| # | 任务 | 说明 |
|---|------|------|
| 1 | 删除 `LogLevel.cs`、`AudioEventListener.kt` | 完全未使用的僵尸文件 |
| 2 | 删除 `AudioMixer.silence()`、`PacketType.Heartbeat` | 僵尸方法/预留枚举 |
| 3 | 删除 `LinkType.Unknown/Bluetooth/Usb`、`TransportType` 未用值 | 双端只保留在用值 |
| 4 | 删除 `NullTransport` + PlatformFactory 占位分支 | 双端桩实现清除 |
| 5 | 修复 PacketHeader 注释 "14B" → "15B" | 注释与代码不一致 |
| 6 | 统一双端状态转换表 | Win 删 Idle/Disconnected→Connecting；Android 补 Reconnecting→Connecting |
| 7 | 修复 Android `UdpTransport.disconnect()` 不关闭 socket | 端口泄漏缺陷 |

### P1：四级链路高度分离 ✅

| # | 任务 | 说明 |
|---|------|------|
| 1 | 双端创建 `Links/` 目录结构 | WifiLan / WifiDirect / Bluetooth / Usb 四条链路独立文件 |
| 2 | 链路常量分离 | 端口/超时/LinkTypeId 等从 ProtocolTypes 迁移到各链路文件 |
| 3 | `WifiDirectP2pHelper` 迁移 | 从 Core/Adapters/ 迁到 Links/WifiDirect/，硬编码改为链路常量 |
| 4 | Windows `LinkType` 静态类删除 | 迁移到链路文件，ProtocolTypes 只留 PacketType |
| 5 | PlatformFactory 去 switch | 改为 if 判断 + 注释预留链路专属工厂扩展点 |
| 6 | Bluetooth/USB 预留骨架 | 双端各创建 LinkTypeId 常量文件 |

**新增文件结构**：
```
Windows: Links/
├── WifiLan/WifiLanLink.cs
├── WifiDirect/WifiDirectLink.cs + WifiDirectP2pHelper.cs
├── Bluetooth/BluetoothLink.cs
└── Usb/UsbLink.cs

Android: links/
├── wifilan/WifiLanLink.kt
├── wifidirect/WifiDirectLink.kt
├── bluetooth/BluetoothLink.kt
└── usb/UsbLink.kt
```

### P2：API 合规性审查 + 修正 ✅

| # | 违规 | 端 | 修正 |
|---|------|-----|------|
| 1 | `MainWindow.axaml.cs` 直调 `new UdpClient()` | Win | → `PlatformFactory.CreateTransport()` + `ITransport` |
| 2 | `MainWindow.axaml.cs` 直调 `new PacketHeaderAdapter()` | Win | → `PlatformFactory.CreateProtocol()` |
| 3 | `MainWindow.axaml.cs` 硬编码 IP/端口/LinkType | Win | → `WifiDirectLink.*` 链路常量 |
| 4 | `AudioPipeline.kt` 直建 `UdpTransport(...)` | Android | → `PlatformFactory.createTransport(...)` |
| 5 | `HandshakeManager.kt` 3 处直建 `UdpTransport(...)` | Android | → `PlatformFactory.createTransport(...)` |
| 6 | `HandshakeManager.kt` 直建 `PacketHeaderAdapter()` | Android | → `PlatformFactory.createProtocol()` |

**合规状态**：应用层全部通过 PlatformFactory + ITransport + IPacketProtocol 通信，无绕过接口层的平台 API 直调。

### P3：接口层补全（待启动）

按《接口映射设计.md》Phase 5 实现：
- `IAudioCapturer` + Adapter（MicCapturerAdapter / SystemAudioCapturerAdapter / StubCapturer）
- `IAudioRenderer` + Adapter（SpeakerRenderer / CableRenderer / StubRenderer）
- `IDiscovery` + Adapter（NsdDiscovery / MdnsPublisher 适配）
- `INetworkMonitor` + Adapter（从 ReconnectionManager 拆分网络监听）
- `Config` 统一配置（替代分散 AudioConfig）
- `PlatformFactory` 补全工厂方法

### P4：插件化扩展接口预留（待启动）

- `IDataChannel` 通用数据通道接口（投屏/文件/剪切板复用）
- `IStreamHandler` 流式数据处理器接口（音频/视频/文件流）
- 音频管线改为 IStreamHandler 实现注册到通道
- 预留单向→双向扩展点
- 所有新接口命名遵循《接口映射设计.md》

### P5：应用层重构接入（待启动）

- AudioPipeline 改用 IAudioCapturer
- AudioRouter 拆为两个 IAudioRenderer，路由逻辑上移
- ReconnectionManager 拆分（监听 → INetworkMonitor，重连保留）
- LanAudioDiscovery 实现 IDiscovery
- MainWindow/MainActivity 改用 PlatformFactory 统一创建 + 注入

### P6：回归验证（待启动）

- 双端编译零 error
- LAN 直连四管线推流/热切/停止/重启
- 冷启动验证（一帧即停不复现）
- P0 看门狗验证（切桌面后自动恢复）
- 僵尸清单逐项闭环
- API 合规性复查

---

## 🔲 未来开发路线

### 🔴 当前任务 — 四级链路降级
| # | 任务 | 说明 |
|---|------|------|
| 1 | **四级链路降级** | WiFi直连 → WiFi同网 → 蓝牙 → 断开（设计文档：proto/LINK_DOWNGRADE.md） |

### 🟡 功能完备后 — UI 统一开发
| # | 任务 | 说明 |
|---|------|------|
| 2 | Windows 实时仪表盘 | 帧率/丢包率/缓冲水位/连接状态指示器 |
| 3 | Android UI 完善 | 连接状态实时显示、链路等级指示 |
| 4 | Windows 系统托盘 | NotifyIcon 正确实现 |

### 🟢 迭代优化
| # | 任务 | 说明 |
|---|------|------|
| 5 | 安全机制（Token+源IP绑定） | 局域网场景优先级低 |
| 6 | 混音加时钟同步 | 防长时间运行漂移 |
| 7 | Android后台保活加固 | 管线迁移到 Service |
| 8 | 自动化测试/CI | — |

### 🔵 远期
| # | 任务 | 前置条件 |
|---|------|----------|
| 9 | Phase 8-10：UI打磨/苹果端/打包发布 | 以上基本稳定 |

---

## 🔍 外部审查结论（2026-07-13）→ 修复状态

### 严重问题（已全部修复）
| # | 问题 | 修复状态 |
|---|------|----------|
| 1 | 无安全机制 | 🔲 待做（优先级低，局域网场景） |
| 2 | 状态机失效（IDLE→CONNECTING 非法） | ✅ 转换表已修复 |
| 3 | 弱网没闭环（FEC/序号/丢包） | ✅ FEC+JitterBuffer+seq追踪已完成 |

### 架构问题（已解决）
| # | 问题 | 修复状态 |
|---|------|----------|
| 4 | P4抽象层没接入主链路 | ✅ 双端完全通过ITransport/IPacketProtocol通信 |
| 5 | Android后台保活不完整 | 🔲 待做 |
| 6 | 混音无同步机制 | 🔲 待做 |
| 7 | 配置集中不彻底 | ✅ 已删除废弃Config，AudioConfig统一管理 |

### 工程化问题
| # | 问题 | 修复状态 |
|---|------|----------|
| 8 | 无真正系统托盘 | 🔲 待做（UI阶段） |
| 9 | 无自动化测试/CI | 🔲 待做 |
| 10 | Gradle写死本地路径 | 🔲 待确认 |
| 11 | 文档与实际不一致 | ✅ 本次更新 |
| 12 | VB-CABLE驱动再分发无许可证说明 | 🔲 待做 |

---

## 🔑 关键决策记录
| 日期 | 决策 | 原因 |
|------|------|------|
| 2026-07-09 | 关闭LPF改HPF 200Hz | LPF砍高频，HPF去底噪更有效 |
| 2026-07-09 | Opus 64k→128k, complexity 5→10 | 音质提升，局域网带宽充裕 |
| 2026-07-13 | P4迁移回滚 | 架构改造出问题 |
| 2026-07-13 | 项目迁移至GitHub（公开） | 替代本地备份，版本管理清晰 |
| 2026-07-22 | P4重新实施成功 | 分10步渐进接入，双端验证通过 |
| 2026-07-23 | JitterBuffer underrun 不再无条件推进 seq | 冷启动帧到达慢导致 nextPullSeq 飞速前进，后续帧全被丢弃 |
| 2026-07-23 | HELLO 触发 ResetSession | Android 每次 HELLO 重启推流 seq 归零，Windows 必须重置 JitterBuffer |
| 2026-07-23 | 冷启动 2s 淡入 | 掩盖 HAL 冷启动初始 underrun 卡顿，用户验收通过 |
| 2026-07-23 | JitterBuffer 5帧预缓冲/24帧容量 | 平衡快速出声与冷启动抗抖动 |
| 2026-07-22 | JitterBuffer 3帧/60ms预缓冲 | 平衡延迟与抗抖动（已被 07-23 更新替代） |
| 2026-07-22 | 热切150ms防抖 | 快速切换只执行最后一次，避免设备反复stop-start |
| 2026-07-22 | UI推迟到链路降级完成后 | 确保全部功能完备后统一写UI |

---

## 🔧 构建与运行

### Windows端
```
cd windows/LanAudioBridge.Desktop
dotnet build -c Release
dotnet run
```

### Android端
```
cd android
./gradlew assembleDebug
```

### 运行环境
- Windows：.NET 10 运行时、VB-CABLE 虚拟音频驱动（archive/ 中含安装包）
- Android：Android SDK、支持 MediaProjection 的设备
- 网络：局域网互通，UDP 12345（音频）/ 12347（控制）端口开放
- 启动顺序：先启 Windows 接收端 → Android 端 mDNS 发现并连接 → 选择模式推流

### 已知限制
- 系统音频采集强制锁媒体音量为0（暴力方案）
- 无真正系统托盘（关窗口=隐藏，需任务管理器退出）
- mDNS 在纯 WiFi Direct（无路由器）环境下可能不可用（重连用 lastKnownHost 兜底）

---

## 性能参数

| 项目 | 当前值 |
|------|--------|
| AudioRecord缓冲 | ~80ms（FRAME_BYTES * 4） |
| MediaProjection缓冲 | ~640ms（FRAME_BYTES * 32） |
| Win BufferMs | **100ms**（从300逐档下调，稳定最低值） |
| Win DesiredLatency | **100ms**（WaveOutEvent） |
| Win Mic latency | **50ms**（WasapiOut CABLE Input） |
| JitterBuffer | **100ms**（5帧预缓冲，24帧容量，冷启动自适应阈值） |
| 热切防抖 | **150ms**（debounce，只执行最后一次） |
| 淡入 | 模式切换 50ms；冷启动 **2秒**（100帧线性 0→1） |
| 音频帧 | 20ms（960采样点 @ 48kHz） |
| 看门狗超时 | 3000ms（触发 Reconnecting） |
| **端到端延迟** | **~200-260ms**（含 JitterBuffer 60ms） |

---

## 四层架构总览

| 层 | Windows | Android | 职责 |
|----|---------|---------|------|
| 接口层 | Core/Interfaces/ | core/interfaces/ | ITransport, IPacketProtocol, ILogger |
| 适配器层 | Core/Adapters/ + Core/Impl/ | core/adapters/ + core/impl/ | UdpTransport, PacketHeaderAdapter, ConsoleLogger/LogcatLogger, NullTransport |
| 基础设施层 | Core/Infrastructure/ + Core/Factory/ | core/infrastructure/ + core/factory/ | Log门面, PlatformFactory |
| 应用层 | 根目录 | audio/ + net/ | AudioEngine, HandshakeServer, AudioRouter, JitterBuffer / AudioPipeline, HandshakeManager, ReconnectionManager |
| UI层 | MainWindow.axaml | MainActivity.kt | Avalonia / Jetpack Compose |

---

## 一句话总结

> **WiFi 直连大重构 P0-P2 已验收（僵尸清理/链路分离/API合规），P3 接口层补全待启动；冷启动优化待 UI 完成后收尾。**
