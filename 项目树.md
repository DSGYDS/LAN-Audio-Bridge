# LAN Audio Bridge — 项目树 & 代码清单

> 最后更新：2026-07-23
> 当前状态：四条链路全通，核心音频通路完全解耦，UI 待启动

---

## 📁 根目录
```
项目树.md              ← 本文档
项目进度存档.md          ← 开发进度、卡点、路线图
局域网音频桥项目计划书.md  ← 完整项目计划书
README.md
MEMORY.md
```

---

## 📁 文档 (`memory/`, `proto/`, `.qoder/specs/`)
```
proto/
  接口映射设计.md         ← 接口定义与映射关系
  开发顺序.md             ← 各阶段开发顺序
  PROTOCOL.md            ← 通信协议规范
  WIFI_DIRECT.md         ← WiFi Direct 方案
  LATENCY_TUNING.md      ← 延迟调优
  RECONNECT.md           ← 断线重连
  LINK_DOWNGRADE.md      ← 链路降级方案

memory/
  2026-07-04.md ~ 2026-07-15.md  ← 开发日志

.qoder/specs/
  WiFi_Direct_Transport_task-953.md  ← Qoder 实施方案
```

---

## 📁 Windows 端 (C#, Avalonia, .NET 10)

### 应用层 — 核心音频处理
```
AudioEngine.cs             ← 引擎编排（190行）
AudioRouter.cs             ← 四模式路由
AudioWatchdog.cs           ← 500ms 超时看门狗
OpusDecodePipeline.cs      ← Opus 解码 + FEC + PLC
PlaybackScheduler.cs       ← 20ms 播放调度
JitterBuffer.cs            ← 抖动缓冲
HandshakeServer.cs         ← LAN 握手服务
ConnectionStateManager.cs  ← 连接状态机
MdnsPublisher.cs           ← mDNS 服务发布
PacketHeader.cs            ← 协议头编解码
ProtocolTypes.cs           ← PacketType 枚举
AudioConfig.cs             ← 音频配置
QrCodeHelper.cs            ← QR 码生成
PairedDeviceStore.cs       ← P2P 配对存储（预留）
```

### UI 层
```
MainWindow.axaml           ← 主窗口布局
MainWindow.axaml.cs        ← UI 事件 + 生命周期
App.axaml / App.axaml.cs   ← 应用入口
Program.cs                 ← 启动入口
```

### Links 层 — 四级链路
```
Links/
├── ILink.cs               ← 统一链路接口
├── LinkManager.cs         ← 纯路由器（~50行）
│
├── WifiLan/
│   └── WifiLanLink.cs     ← LAN 链路完整实现
│
├── WifiDirect/
│   ├── WifiDirectLink.cs          ← P2P 链路
│   └── WifiDirectP2pHelper.cs     ← P2P 建链助手
│
├── Bluetooth/
│   ├── BluetoothLink.cs           ← 蓝牙链路
│   └── BtPassiveHandshake.cs      ← 蓝牙被动握手（新）
│
└── Usb/
    ├── UsbLink.cs                 ← USB 链路
    └── UsbPassiveHandshake.cs     ← USB 被动握手（新）
```

### Core 层 — 传化层（P0-P6 完成）
```
Core/Interfaces/           ← 9 个接口
   IAudioCapturer.cs
   IAudioRenderer.cs
   IDataChannel.cs
   IDiscovery.cs
   ILogger.cs
   INetworkMonitor.cs
   IPacketProtocol.cs
   IStreamHandler.cs
   ITransport.cs

Core/Adapters/             ← 9 个适配器
   PacketHeaderAdapter.cs
   UdpTransport.cs
   SpeakerRenderer.cs
   CableRenderer.cs
   StubCapturer.cs
   WinDiscovery.cs
   WinNetworkMonitor.cs
   BluetoothTransport.cs            ← 蓝牙传输（新）
   UsbTransport.cs                  ← USB 传输（新）

Core/Enums/
   CapturerType.cs
   NetworkQuality.cs
   StreamType.cs
   TransportType.cs

Core/Factory/
   PlatformFactory.cs

Core/Infrastructure/
   Log.cs

Core/Impl/
   ConsoleLogger.cs

Core/Models/
   DeviceInfo.cs
   NetworkInfo.cs
```

**统计：60 个 .cs 文件**

---

## 📁 Android 端 (Kotlin, Jetpack Compose)

### 应用层
```
MainActivity.kt                    ← UI 入口（~36 行纯壳）
ConnectionStateManager.kt          ← 连接状态机
StreamingService.kt                ← 前台 Service
MediaProjectionService.kt          ← 媒体投影 Service
AudioPermissionManager.kt          ← 权限管理
```

### 音频层
```
audio/
├── AudioPipeline.kt          ← 编排（93行）
├── CaptureLoop.kt            ← 采集循环 + 看门狗
├── EncodeSender.kt           ← 编码发送
├── PcmFrameAssembler.kt      ← PCM 拼帧
├── AudioEncoder.kt
├── AudioMixer.kt
├── AudioConfig.kt
├── MicrophoneCapturer.kt
└── SystemAudioCapturer.kt
```

### 网络层
```
net/
├── HandshakeManager.kt       ← LAN 握手
├── LanAudioDiscovery.kt      ← mDNS 发现
├── ReconnectionManager.kt    ← 断线重连
├── WifiDirectManager.kt      ← P2P 管理
├── PacketHeader.kt
├── ProtocolTypes.kt
└── P2pPairStore.kt           ← 配对存储（预留）
```

### Links 层 — 四级链路
```
links/
├── ILink.kt                  ← 统一链路接口
├── LinkManager.kt            ← 纯路由器（~50行）
│
├── wifilan/
│   └── WifiLanLink.kt        ← LAN 链路完整实现
│
├── wifidirect/
│   ├── WifiDirectLink.kt     ← P2P 链路完整实现
│   └── P2pHandshakeServer.kt ← P2P 专属握手服务
│
├── bluetooth/
│   ├── BluetoothLink.kt      ← 蓝牙链路
│   └── BtHandshakeClient.kt  ← 蓝牙握手客户端（新）
│
└── usb/
    ├── UsbLink.kt            ← USB 链路
    └── UsbHandshakeClient.kt ← USB 握手客户端（新）
```

### UI 层
```
ui/
├── TestUI.kt                 ← 场景卡片 & 状态显示
├── QrScannerScreen.kt        ← CameraX + ML Kit 扫码
└── theme/Theme.kt            ← 主题
```

### Core 层 — 传化层（P0-P6 完成）
```
core/interfaces/              ← 9 个接口（与 Win 一一对应）
   IAudioCapturer.kt
   IAudioRenderer.kt
   IDataChannel.kt
   IDiscovery.kt
   ILogger.kt
   INetworkMonitor.kt
   IPacketProtocol.kt
   IStreamHandler.kt
   ITransport.kt

core/adapters/                ← 9 个适配器
   PacketHeaderAdapter.kt
   UdpTransport.kt
   MicCapturerAdapter.kt
   SystemAudioCapturerAdapter.kt
   StubRenderer.kt
   NsdDiscoveryAdapter.kt
   AndroidNetworkMonitor.kt
   BluetoothTransport.kt              ← 蓝牙传输（新）
   UsbTransport.kt                    ← USB 传输（新）

core/enums/
   CapturerType.kt
   LogLevel.kt
   NetworkQuality.kt
   StreamType.kt
   TransportType.kt

core/factory/
   PlatformFactory.kt

core/impl/
   LogcatLogger.kt

core/infrastructure/
   Log.kt

core/models/
   DeviceInfo.kt
   NetworkInfo.kt
```

**统计：61 个 .kt 文件**

---

## 📋 版本里程碑

| 版本 | 内容 | Commit | 日期 |
|------|------|--------|------|
| P0 | 僵尸清理 + 缺陷修复 | `9f21437` | 07-23 |
| P1 | 四级链路分离 | `0b6af4a` | 07-23 |
| P2 | API 合规审查 + 卡顿修复 | `e448799` | 07-23 |
| P3 | 接口层补全（9 接口） | `018eb10` | 07-23 |
| P4 | 插件化扩展预留 | `d94c982` | 07-23 |
| P5 | 应用层重构接入 | `7fb21e1` | 07-23 |
| P5.5 | LinkManager 纯路由 + 链路自治 | `ef61656` | 07-23 |
| P5.5.5 | 核心音频通路拆解 | `af0b304` | 07-23 |
| P6 | 回归验证全量通过 | `c309551` | 07-23 |
| **蓝牙** | 蓝牙链路完全解耦 | `e8d59dd` | 07-23 |
| **四条链路** | WiFi LAN + Direct + 蓝牙 + USB 全通 | `d97c211` | 07-23 |
| **验收** | 除 bug 和驱动外全部完成 | `0b6d8e4` | 07-23 |

---

## 📌 核心架构总览

```
┌─────────────────────────────────────────────────────┐
│                    UI 层（待启动）                    │
│  场景卡片: 🏠在家 / 🌐万能 / 🔵蓝牙 / 🔌USB          │
├─────────────────────────────────────────────────────┤
│                   Links 层（四条链路自治）             │
│  ┌──────────┐ ┌──────────┐ ┌────────┐ ┌────────┐   │
│  │ WifiLan  │ │ WifiDirect│ │Bluetooth│ │  Usb   │   │
│  └──────────┘ └──────────┘ └────────┘ └────────┘   │
│  LinkManager ← 纯路由器，when(linkType) 分发          │
├─────────────────────────────────────────────────────┤
│                 Core 接口层（9接口）                   │
│  ITransport / IAudioCapturer / IAudioRenderer /     │
│  IDiscovery / INetworkMonitor / IPacketProtocol /   │
│  ILogger / IDataChannel / IStreamHandler            │
├─────────────────────────────────────────────────────┤
│                Core 适配器层（9适配器）                │
│  UdpTransport / BlueTransport / UsbTransport /      │
│  MicCapturerAdapter / SpeakerRenderer / ...         │
├─────────────────────────────────────────────────────┤
│             核心音频通路（P5.5.5 拆解完成）            │
│  AudioEngine (Win) / AudioPipeline (Android)        │
│  → OpusDecodePipeline → PlaybackScheduler → AudioRouter │
│  → CaptureLoop → EncodeSender → PcmFrameAssembler     │
└─────────────────────────────────────────────────────┘
```

---

**总计：121 个核心源文件（60 C# + 61 Kotlin／6 层架构／4 种链路）**
